---
interface Props {
  text?:
    | 'Simulação visual'
    | 'Aplicação ilustrativa'
    | 'Mockup demonstrativo'
    | 'Imagem gerada por IA';
}

const { text } = Astro.props;
---

{text && <p class="disclosure"><span aria-hidden="true">i</span>{text}</p>}
