<figure class="logo-intro" data-logo-intro>
  <svg viewBox="0 0 720 520" role="img" aria-labelledby="placeholder-title placeholder-description">
    <title id="placeholder-title">Espaço reservado para o logo oficial</title>
    <desc id="placeholder-description">
      Composição geométrica neutra usada enquanto os arquivos SVG originais não foram fornecidos.
    </desc>
    <g class="logo-layer logo-layer--construction" fill="none" stroke="currentColor" stroke-width="2">
      <path d="M85 85h235v235H85zM400 125h210v210H400zM175 390h370" />
      <path d="m85 85 235 235M320 85 85 320M400 125l210 210M610 125 400 335" />
    </g>
    <g class="logo-layer logo-layer--outline" fill="none" stroke="currentColor" stroke-width="8">
      <circle cx="202" cy="202" r="112" />
      <circle cx="505" cy="230" r="100" />
    </g>
    <g class="logo-layer logo-layer--texture" fill="currentColor">
      {Array.from({ length: 9 }, (_, index) => (
        <circle cx={180 + index * 42} cy="390" r="5" />
      ))}
    </g>
    <g class="logo-layer logo-layer--final" fill="currentColor">
      <rect x="85" y="445" width="525" height="6" />
    </g>
  </svg>
  <figcaption>Placeholder neutro — SVG oficial pendente</figcaption>
</figure>
